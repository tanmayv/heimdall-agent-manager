# Deprecated daemon-only ham-ctl surface

Legacy implementations are parked in `../daemon_legacy.odin` and intentionally not wired from `main.odin`. Re-enable only by porting commands to Hub `/api/v1` or Bridge agent-mode with bearer-only auth.
